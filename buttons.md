# Чек-лист: постоянное меню + генерация фото (проверка вживую)

Код уже задеплоен на сервер (коммит `1f33c4d`), сервис `content-bot` перезапущен
и работает. Ниже — 10 шагов ручной проверки в реальном Telegram-аккаунте.
Автотестами это не проверяется (нужен настоящий бот и настоящий чат).

## Шаги проверки

1. **Деплой.** Уже выполнен — `sudo systemctl status content-bot` на сервере
   показывает `active (running)`, запущен без ошибок.

2. **Старый чат без кнопки СТАРТ.** Открой чат с ботом, где уже есть история
   сообщений (не новый чат) — убедись, что стандартной синей кнопки
   "START" от Telegram больше НЕТ. Это как раз та проблема, которую чинили.

3. **Первый `/start`.** Отправь `/start` один раз — должно появиться:
   - приветствие, а под ним внизу экрана — постоянная кнопка **«Старт»**;
   - следующим сообщением — **«Что делаем дальше?»** с двумя кнопками:
     **«Я умею»** и **«Создать пост сейчас»**.

4. **Кнопка «Старт» работает как /start.** Закрой чат, открой заново, нажми
   кнопку **«Старт»** внизу экрана (не набирай `/start` руками) — должно
   появиться то же самое приветствие + меню.

5. **«Я умею».** Нажми — должен появиться текст с описанием возможностей:
   сайт, генерация фото, авторский стиль, 4 языка.

6. **«Создать пост сейчас».** Нажми — должны появиться три кнопки:
   **«МОЙ САЙТ»**, **«Генерация текста»**, **«Генерация фото»**.

7. **«МОЙ САЙТ».** Нажми — сайт должен открыться прямо внутри Telegram
   (как Mini App), а не в отдельном браузере.

8. **«Генерация текста».** Нажми — появится подсказка; затем пришли реальную
   ссылку/голосовое/текст — обычная генерация поста должна работать как раньше.

9. **«Генерация фото».** Нажми, опиши картинку словами — бот должен
   сгенерировать и прислать фото, а затем предложить прислать текст/ссылку/
   голосовое для поста.

10. **Публикация с фото.** После шага 9 пришли текст — бот подготовит варианты
    поста, а кнопка **«Опубликовать в канал»** должна выложить пост **с фото
    из шага 9** прикреплённым.

## Дополнительно: проверка исправленного бага

11. **Кнопка «Старт» — аварийный выход.** Нажми «Генерация фото», но вместо
    описания картинки отправь любую команду (например `/site` или снова
    «Старт»). Ожидается: бот НЕ должен воспринять команду как описание
    картинки и НЕ должен запускать платную генерацию — вместо этого должно
    открыться обычное меню. (Раньше это было багом: бот застревал в режиме
    ожидания описания и любое сообщение — включая команды — трактовал как
    запрос на генерацию.)

## Что делать при расхождении

Если какой-то пункт не совпал — напиши номер шага и что увидела вместо
ожидаемого (текст сообщения, скриншот). VK-автопубликация в этот чек-лист
не входит — это отдельный проект в будущем.

## Журнал сессии

С этого момента сюда записывается всё, что делаем по теме кнопок/Telegram-скиллов.

### 2026-07-26

- Искали на GitHub Claude Code skills, нужные для создания кнопок в ТГ-приложении.
  Нашли исходник с реальным содержимым (не заглушку) в репозитории
  `davila7/claude-code-templates`.
- Скачали в проект (project-scoped, `.claude/skills/`, глобальную папку `~/.claude/skills`
  не трогали):
  - `telegram-bot-builder` — SKILL.md + `reference/api_methods.md`, `api_types.md`,
    `patterns_and_examples.md` (Bot API: inline/reply-кнопки, callback query, webhooks,
    медиа, платежи; Node.js и Python — python-telegram-bot/aiogram).
  - `telegram-mini-app` — SKILL.md (Web App API, MainButton/BackButton, initData) —
    пригодится для кнопки «МОЙ САЙТ», которая открывает сайт как Mini App внутри Telegram.
- Договорённость: этот скилл внедряется точечно, только в проекты, которым он реально
  нужен, а не во все подряд — чтобы не путаться. Источник и договорённость сохранены
  в памяти ассистента (`feedback_skill_scoping.md`, `reference_telegram_skill_source.md`).
- Закоммичено: `7ab2b43` (скиллы), `3982c75` (этот файл + `picture.md`).

### 2026-07-26 (продолжение) — нативная menu-button кнопка «Старт»

- Запрос: сделать так, чтобы при входе в бота слева внизу была кнопка «Старт»,
  без необходимости писать `/start` каждый раз.
- Выяснили: persistent-кнопка «Старт» (reply-клавиатура, `bot/keyboards/start.py`)
  уже была сделана раньше (коммит `1f33c4d`) — она появляется в ответ на первый
  `/start` (в т.ч. когда пользователь жмёт нативную синюю кнопку START в профиле
  бота — Telegram сам шлёт `/start` при тапе) и дальше работает вместо команды.
  Это уже покрывает «не писать /start каждый раз».
- Уточнили у владельца, что дополнительно нужна нативная Telegram menu-button —
  отдельная маленькая кнопка слева от поля ввода (иконка меню команд), которая
  видна сразу при входе в чат, ещё до первого сообщения, и не зависит от
  reply-клавиатуры.
- Сделано:
  - `bot/locales/{ru,en,vi,zh}.py` — добавлен ключ `command_start_description`
    (локализованное описание команды `/start` для меню).
  - `bot/main.py` — новая функция `_configure_start_menu_button(bot)`: регистрирует
    команду `/start` через `bot.set_my_commands(...)` для каждого из 4 языков
    (`language_code=lang`) и включает `bot.set_chat_menu_button(menu_button=MenuButtonCommands())`.
    Вызывается в `run()` сразу после создания `Bot`, до старта polling.
  - `tests/test_main_site_api_wiring.py` — новый тест
    `test_run_configures_native_menu_button_before_polling`, проверяет вызовы
    `set_my_commands`/`set_chat_menu_button` с правильными аргументами.
- Полный прогон тестов: `290 passed`.
- **Задеплоено.** Запушено в `olgashomrina/feature/mvp1-bot` (`8a62747`), на
  сервере `147.45.175.189`: `git pull` (уже был в актуальном состоянии) →
  `.venv/bin/pip install -r requirements.txt` → `systemctl restart content-bot`.
  Сервис поднялся чисто, `active (running)`, в логах ошибок от
  `set_my_commands`/`set_chat_menu_button` нет — значит вызовы к Bot API
  прошли успешно на реальном боевом токене.
  **Владельцу проверить вживую:** открыть чат с ботом — слева от поля ввода
  должна появиться иконка меню с командой «/start». Если иконка не
  появилась сразу — попробовать закрыть и заново открыть чат (клиент
  Telegram иногда не подхватывает такие изменения без перезахода).
