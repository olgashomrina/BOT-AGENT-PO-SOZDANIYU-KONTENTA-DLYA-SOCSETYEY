# Деньги: себестоимость ИИ-вызовов и защита баланса

Журнал работы по экономике бота — цены, лимиты, учёт расхода.

## 2026-07-30

### Что искали

Владелец сообщила: голосовые сообщения не работают, в параллельной сессии codex
пишет о недостатке денег на vsegpt.ru. Заявлено: «заплатила за безлимитный
тариф, не хватает даже на тест». Цель — безлимитная работа бота при расходе
не больше 200 ₽/мес.

### Что нашли

**1. Локальный `.env` сломан.** В строке 12 `AI_PROXY_API_KEY` содержит токен
Telegram-бота, а не ключ VseGPT (оба начинаются с `8819677652:A…` — копипаста).
Проверено живыми запросами к `api.vsegpt.ru`:

```
GET  /balance                → 403 {"error": {"message": "Incorrect API key.", "code": 403}}
POST /audio/transcriptions   → 403 {"error": {"message": "Incorrect API key.", "code": 403}}
POST /chat/completions       → 403 {"error": {"message": "Incorrect API key.", "code": 403}}
```

Локально не работает ни голос, ни текст, ни картинки — и причина не в деньгах.
Нужен реальный ключ VseGPT (`sk-or-vv-…`) из личного кабинета.

**2. На проде (Timeweb) — действительно баланс.** Проверить напрямую из этой
сессии нельзя (серверный `.env` недоступен), но данные codex совпадают с уже
зафиксированным в `bot/config.py:22-26` наблюдением от 29.07: `img-flux/pro1.1`
незаметно слил баланс.

**3. Тариф VseGPT не оплачивает использование API.** Подписка (199 ₽ Базовый /
399 ₽ Профессиональный за 30 дней) даёт доступ к моделям, снятие лимитов на
частоту запросов и автопополнение. «Неограниченные запросы» в их описании — про
их собственный веб-чат. Каждый вызов API списывается с баланса по факту.
Это корень недоразумения «заплатила за безлимит, а денег нет».

### Цены (живой каталог `api.vsegpt.ru/v1/models`, 30.07.2026)

Тарификация STT: 1 токен = 1 секунда аудио, цена указана за 1000 токенов.

| Операция | Цена каталога | В пересчёте |
|---|---|---|
| `stt-openai/whisper-1` | 20.00 / 1000 с | **1.20 ₽/мин** |
| `stt-openai/gpt-4o-mini-transcribe` | 16.00 / 1000 с | **0.96 ₽/мин** |
| `stt-openai/gpt-4o-transcribe` | 32.00 / 1000 с | 1.92 ₽/мин |
| `stt-deepgram/nova-3` | 20.00 / 1000 с | 1.20 ₽/мин |
| `openai/gpt-4o-mini` | 0.02 / 0.08 за 1000 токенов | ~0.25 ₽ за 2 варианта поста |
| `img-flux/flux-2-klein-4b` | 3.9 | **3.90 ₽/картинка** |
| `img-flux/pro1.1` | 10.0 | **~10–15 ₽/картинка** |

Полный цикл «голосовое 1 мин → 2 поста → 1 картинка»: ~5.4 ₽ с дешёвой
картинкой, ~12–17 ₽ с pro1.1. **Голос — 8–20 % чека, картинки — 70–90 %.**
На 200 ₽: ~160 циклов без картинок, ~37 с klein, ~13 с pro1.1.

### Альтернативы для распознавания речи

| Вариант | ₽/мин | Подвох |
|---|---|---|
| VseGPT `whisper-1` (было) | 1.20 | — |
| VseGPT `gpt-4o-mini-transcribe` | 0.96 | −20 %, одна строка конфига |
| SaluteSpeech (Сбер) | 0 до 100 мин/мес, далее 1.20 | 100 мин на аккаунт, не на юзера |
| Yandex SpeechKit | дороже; точную цену не подтвердили (страница под капчей) | пробный месяц |
| Groq `whisper-large-v3-turbo` | ~0.06 | оплата в USD, доступ с РФ-сервера под вопросом |
| faster-whisper на своём VPS | 0, безлимит | текущий Timeweb 1 vCPU / 1 ГБ слаб, нужен апгрейд до 2 vCPU / 4 ГБ + ffmpeg |

Вывод: «безлимитного Whisper» за 200 ₽/мес не продаёт никто. 200 ₽ — это
бюджет на тесты владельца. Настоящий безлимит на голос даёт только
self-hosted faster-whisper (фиксированная плата за сервер вместо поминутной).

### Решения владельца

- Голос: остаёмся на VseGPT, но переключаем на `gpt-4o-mini-transcribe`
  + лимит длины голосового.
- Ограничители: выбор делегирован ассистенту → берём все четыре (лимит длины
  голосового, дневной лимит картинок на пользователя, предупреждение владельцу
  о низком балансе, учёт расхода в рублях по пользователю).

### Что сделано в коде

1. **Модель распознавания** → `stt-openai/gpt-4o-mini-transcribe` по умолчанию
   (0.96 ₽/мин вместо 1.20 ₽/мин). `bot/config.py`, `.env.example`.
2. **Лимит длины голосового** — `MAX_VOICE_DURATION_SECONDS`, по умолчанию 180 с.
   Проверка в `bot/handlers/content.py::_reject_overlong_voice` стоит до
   скачивания файла, так что отклонённое голосовое не стоит ничего. Новые строки
   `error_voice_too_long` во всех четырёх локалях.
3. **Дневной лимит картинок на пользователя** — `DAILY_IMAGE_LIMIT`, по
   умолчанию 3. Отдельный счётчик в новой таблице `image_usage_log` (картинка
   стоит ~16 постов текста, общая квота её не сдерживает). Правило в одном месте
   — `bot/handlers/image_budget.py::ensure_image_budget`, подключено ко всем трём
   местам генерации: кнопка «Картинка от ИИ», кнопка «Сделать реалистичнее»,
   генерация фото из меню `/start`. В upgrade-пути проверка стоит **до**
   `claim_image_prompt`, чтобы отказ не съедал одноразовый промпт.
4. **Предупреждение о балансе** — `bot/services/balance_watcher.py`, проверка раз
   в час (`BALANCE_CHECK_INTERVAL_SECONDS`), порог 50 ₽
   (`BALANCE_ALERT_THRESHOLD_RUB`). Пишет владельцу один раз на пересечение
   порога, а не каждый час, и снова вооружается, когда баланс поднялся.
   В тексте — на сколько картинок/минут голоса хватит остатка и напоминание,
   что тариф не оплачивает использование API.
   `ai_gateway.get_balance()` разбирает ответ `GET /balance` терпимо: формат
   ответа vsegpt.ru не документирован и живым ключом проверить не удалось
   (единственный доступный ключ отклонён), поэтому парсер ищет число под
   ключом вида `balance`/`credit`/`amount` на любой глубине, а при неудаче
   логирует сырое тело — так реальную форму можно будет уточнить с прода.
5. **Учёт расхода в рублях** — `bot/services/cost_tracker.py` (снимок цен
   каталога), таблица `cost_log`, `bot/storage/costs.py`, команда `/costs`
   (только владельцу, `bot/handlers/costs.py`): итог за месяц, разбивка по
   операциям и по пользователям. Учитываются картинки и расшифровка;
   текст постов (~0.25 ₽ за цикл, <5% расхода) намеренно не детализируется —
   это потребовало бы прокинуть счётчики токенов через каждый вызов шлюза.
   Об этом сказано и в самом отчёте `/costs`.

Тесты: 525 зелёных (было 516 до этой работы).

### Что осталось на стороне владельца (по состоянию на 30.07)

- **Починить локальный `.env`**: подставить настоящий ключ VseGPT
  (`sk-or-vv-…`) в `AI_PROXY_API_KEY` вместо токена бота.
- **Пополнить баланс VseGPT** — 200 ₽ это ~160 циклов без картинок, ~37 с
  дешёвой картинкой, ~13 с pro1.1.
- Проверить на проде, что серверный `.env` содержит правильный ключ, и
  задеплоить эти изменения (миграции не нужны — обе новые таблицы создаются
  через `CREATE TABLE IF NOT EXISTS` в `init_db`).

## 2026-07-31

### Баланс прода измерен напрямую — гипотеза подтверждена

Переезд на Beget отменён (Timeweb оплачен до 14.09.26), вернулись к деньгам.
Запрос выполнен **на самом сервере**, чтобы ключ не покидал его:

```bash
ssh root@147.45.175.189 'set -a; . /opt/content-bot/.env; set +a;
  curl -s -H "Authorization: Bearer $AI_PROXY_API_KEY" https://api.vsegpt.ru/v1/balance'
```

Ответ:

```json
{"status":"ok","data":{"credits":"0.017680","subscription_status":"ok",
 "subscription_end":"2026-08-22 07:34:58","user_status":2,
 "user_status_text":"Less than 10 credits on account. Less than 4.0 credits on account. "}}
```

**Баланс — 0.0177 ₽.** Подписка при этом активна до 22.08.2026 — ровно та
картина, из-за которой возникло недоразумение «заплатила за безлимит, а не
работает»: подписка жива, денег на использование нет.

Ключ на проде правильный (`sk-or-vv-…`, 73 символа) — в отличие от локального
`.env`. Модель распознавания на проде пока `stt-openai/whisper-1`: новый код
ещё не задеплоен.

Проверочный вызов генерации показал точный отказ:

```json
{"error": {"message": "Potentially out of budget: 32->700, expected price 0.05664,
 but you have only 0.017680 on account. Please, add some money to balance
 to proceed: https://vsegpt.ru/User/Money", "code": 400}}
```

Заодно выяснен формат `GET /balance`, который вчера не удалось проверить:
авторизация — заголовком `Authorization: Bearer`, число лежит в
`data.credits` **строкой**. Написанный вчера терпимый парсер эту форму
разбирает верно; теперь она закреплена тестом на реальном ответе.
Другие варианты авторизации (`?api_key=`, `x-api-key`, POST) не работают.

### Что сделано в коде

6. **Отдельная ошибка «кончились деньги»** — `AIGatewayOutOfBudgetError`.
   Раньше отказ по балансу приходил как HTTP 400 и попадал в общий
   `AIGatewayInvalidResponseError`, а пользователь видел «не удалось получить
   корректный ответ от ИИ-модели» — то есть ровно то сообщение, из-за которого
   день ушёл на поиск бага в боте вместо пополнения счёта. Теперь 4xx с
   маркерами (`out of budget`, `add some money`, `insufficient`, …)
   распознаётся отдельно, не ретраится (ответ не изменится, пока нет денег) и
   показывает пользователю честное «сервис временно недоступен, владелец
   уведомлён» — новый ключ `error_out_of_budget` во всех четырёх локалях.
   Подключение к хендлерам — одной строкой в `_AI_ERROR_KEYS`, поэтому
   действует сразу во всех местах генерации.

Тесты: 530 зелёных (было 525).

### Что осталось на стороне владельца

- **Пополнить баланс VseGPT** — https://vsegpt.ru/User/Money. Сейчас там
  0.0177 ₽, этого не хватает даже на один вызов. 200 ₽ = ~160 циклов без
  картинок, ~37 с дешёвой картинкой, ~13 с pro1.1.
- **Задеплоить накопленные изменения на прод** (5 коммитов не отправлены на
  GitHub; пуш запускает автодеплой на Timeweb).
- Локальный `.env` — для прода не нужен, только для запуска бота на своём
  компьютере.

### Переезд на openrouter.ai

Владелец решила уйти с VseGPT. У неё уже оплачен OpenRouter — проверено
живым запросом с её ключом: `{"data": {"total_credits": 20,
"total_usage": 1.2842976}}`, то есть **$18.72 ≈ 1721 ₽**. Против 0.0177 ₽
на VseGPT, так что переезд заодно чинит и текущий простой.

**Все три эндпоинта совместимы — код запросов менять не пришлось.**
Проверено вживую тем же форматом, что шлёт бот:

| Что | Результат | Цена |
|---|---|---|
| `/chat/completions` | 200 | 13.8 ₽/1M токенов против 20 ₽ |
| `/audio/transcriptions`, `openai/whisper-1` | 200, `{"text":…, "usage":{"seconds":1,"cost":0.0001}}` | 0.55 ₽/мин против 1.20 ₽ |
| `/audio/transcriptions`, `openai/gpt-4o-mini-transcribe` | 200 | ~0.28 ₽/мин против 0.96 ₽ |
| `/images/generations`, `openai/gpt-5-image-mini` | 200, `data[0].b64_json` — ровно то, что бот разбирает | **0.78 ₽/картинка против 3.90 ₽** |

Слаги у OpenRouter без приставки `stt-`. Оплата только в долларах (карты РФ
не работают, официально — USDC с комиссией 5%), поэтому VseGPT остаётся
запасным вариантом: `.env.example` описывает оба блока настроек целиком.

### Что сделано в коде

7. **Проверка баланса под провайдера.** У OpenRouter это `GET /credits`, и
   там ловушка: `total_credits` — это сумма всех пополнений за всё время, а
   остаток равен `total_credits - total_usage`. Вчерашний терпимый парсер
   вернул бы 20 вместо 18.72, и предупреждение о низком балансе не сработало
   бы никогда. Поэтому у OpenRouter явный читатель, а не обход JSON.
8. **Валюта.** `get_balance()` теперь всегда возвращает рубли: OpenRouter
   считает в долларах, а весь бюджет проекта — рублёвый (порог
   предупреждения, `/costs`, оценки «хватит на N картинок»). Пересчёт по
   `USD_RUB_RATE` (по умолчанию 92) — грубая константа для предупреждений,
   по ней ничего не списывается.
9. **402 Payment Required** — распознаётся как «кончились деньги» по одному
   коду, без разбора тела: так OpenRouter сообщает о пустом счёте, тогда как
   VseGPT отдаёт 400 с объяснением в теле.
10. **Формат аудио определяется по байтам** (`_audio_part_metadata`).
    Найдено при сквозной проверке: бот подписывал **любое** аудио как
    `telegram-voice.ogg` / `audio/ogg`. Для голосовых это верно, но
    `handle_voice` принимает и `message.audio` — то есть mp3, m4a, wav,
    которые уезжали с враньём в метаданных. VseGPT это прощал, OpenRouter
    отвечает голым `Provider returned 400`. Теперь контейнер определяется по
    сигнатуре (OggS/RIFF/fLaC/ID3/ftyp/EBML) с откатом на ogg.
11. **Цены OpenRouter** добавлены в `cost_tracker` — замеренные, не из
    документации.

Сквозная проверка через собственный код бота (`load_settings` →
`get_balance` → `generate_text` → `transcribe`): баланс 1721.05 ₽, текст
генерируется, расшифровка доходит до модели. Тесты: 587 зелёных.

### Осталось

- Переключить прод: серверный `.env` (адрес, провайдер, ключ, слаги моделей)
  и деплой накопленных коммитов.
- Отменить автопродление подписки VseGPT (активна до 22.08.2026), иначе
  спишется ещё раз за неиспользуемый сервис.

### Переезд на OpenRouter не состоялся: блокировка по IP сервера

Переключение прода выполнено и **откатано** в тот же час. Причина: с сервера
Timeweb (`147.45.175.189`) openrouter.ai отвечает 403 на всё подряд:

```
GET /api/v1/credits  (с ключом)   -> 403 {"success": false, "error": "Access denied by security policy."}
GET /api/v1/models   (без ключа)  -> 403
GET /                (главная)    -> 403
```

Публичный список моделей и главная страница блокируются так же, как
запрос с ключом, и смена User-Agent не помогает — значит блокировка по IP
российского хостинга, а не по ключу и не по аккаунту. С домашнего
компьютера владельца тот же ключ работает без нареканий: там и баланс
читается, и распознавание, и картинки.

Порядок действий был: бэкап `.env` → переключение на OpenRouter → деплой
кода → обнаружен 403 в логах при первой же проверке баланса → восстановление
`.env` из бэкапа → рестарт. Итог: прод снова на vsegpt.ru, сервис `active`,
`/balance` с сервера отвечает 200. Простой — минуты.

**Код при этом остался задеплоенным и он провайдеро-независимый:** лимиты,
предупреждение о балансе, `/costs`, понятная ошибка при нехватке денег и
определение формата аудио работают одинаково с любым прокси.

Варианты дальше:

1. Пополнить vsegpt.ru — бот работает сегодня, больше ничего не меняется.
2. Перенести сервер за пределы РФ — тогда OpenRouter доступен и расходы
   падают в 2–5 раз. Beget этого бы не решил: он тоже российский.
3. Ходить на OpenRouter через прокси с сервера — лишняя точка отказа.

### Где остановились (конец сессии 2026-07-31)

Решение владельца: переезжаем на OpenRouter, для этого нужен сервер вне РФ.

**Следующий шаг — за владельцем:** создать сервер в личном кабинете
timeweb.cloud с локацией **Амстердам или Франкфурт** (не Санкт-Петербург),
Ubuntu 24.04, минимальная конфигурация, публичный IP, SSH-ключ
`github-actions-deploy` выбрать из уже сохранённых в аккаунте. Прислать IP.

**Первым делом после этого — проверка, а не переезд:** европейские серверы
Timeweb принадлежат российской компании, и их адреса теоретически тоже могут
быть в блок-листе OpenRouter. Проверять так:

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://openrouter.ai/api/v1/models
```

200 — переезжаем. 403 — сервер удалить (почасовая оплата, потеря копеечная)
и остаться на vsegpt.ru с пополнением. Тратить время на перенос данных до
этой проверки нельзя.

**Если 200, порядок переезда** — по `deploy/README-beget.md`, он подходит
почти дословно, меняется только провайдер. Ключевое из него: перенести
`.env`, `bot.db`, `site_media/`; остановить и `disable` старого бота **до**
запуска нового (Telegram не допускает двух long-polling клиентов на один
токен); обновить секрет `DEPLOY_HOST` на GitHub, иначе автодеплой продолжит
молча ездить на старый сервер; заново выпустить сертификат для API сайта на
новом IP и поправить `data-api-base` на самом сайте.

Настройки OpenRouter для нового `.env` — в `.env.example`, блок «ВЫБОР
ИИ-ПРОВАЙДЕРА», вариант 1.

**Состояние на 31.07:** прод (`147.45.175.189`) работает на vsegpt.ru с
балансом 0.0177 ₽ — то есть генерация отказывает, пока счёт не пополнен.
Весь новый код задеплоен. Локальный `.env` настроен на OpenRouter и рабочий:
с компьютера владельца бота можно запускать и тестировать полностью.
Старый сервер оплачен до 14.09.2026, подписка vsegpt.ru — до 22.08.2026
(автопродление стоит отменить, если провайдер больше не нужен).

## 2026-08-03

### Переезд состоялся — но на Beget, и OpenRouter так и остался недоступен

Владелец самостоятельно создала и оплатила VPS на Beget (`159.194.214.72`,
2 ядра / 2 ГБ / 30 ГБ NVMe, Ubuntu 24.04, 868 ₽ на счету ≈ 28 дней) и
приняла решение переезжать независимо от результата проверки. Переезд
выполнен полностью.

**Проверка провайдеров с нового сервера (первое, что сделано после входа):**

```
https://openrouter.ai/api/v1/models -> 403
https://openrouter.ai/              -> 403
https://api.vsegpt.ru/v1/models     -> 200
https://api.telegram.org            -> 302
```

Гипотеза от 31.07 подтвердилась: сервер стоит в Раменском (Московская
область, AS198610 Beget LLC), OpenRouter блокирует его так же, как Timeweb.
**Переезд не дал доступа к балансу OpenRouter (~1721 ₽)** — прод остался на
vsegpt.ru, где 0.0177 ₽, то есть генерация по-прежнему отказывает, пока
счёт не пополнен. Задача «уйти на дешёвого провайдера» требует сервера вне
РФ; ни Timeweb-СПб, ни Beget её не решают.

### Как проходил доступ к серверу (полдня ушло именно на это)

Вход по паролю root не работал ни из PowerShell, ни через VNC-консоль
панели. Пароль в панели менялся, но SSH его отвергал. Обходные пути
(вставка в VNC не работает, набор длинных команд руками) владельца
измотали. Сработал файловый менеджер Beget (`sprutio.beget.com`): в нём
`/root/.ssh/authorized_keys` открывается на правку прямо в браузере, где
обычная вставка Ctrl+V работает. Публичный ключ вставлен туда — и доступ
появился сразу.

**Вывод на будущее: при проблемах с паролем на Beget не воевать с консолью,
а класть ключ через файловый менеджер панели.**

### Что перенесено и как проверено

Порядок был именно такой, чтобы не получить двух long-polling клиентов на
один токен:

1. На новом сервере: `python3-venv`, `python3-dev`, `build-essential`,
   `ffmpeg`, `nginx`, `certbot`, `rsync`.
2. Код перенесён `tar` через ssh (без `.venv`, `logs`, `tmp_media`) — 6.8 МБ.
3. `venv` на системном Python 3.12.3 (на старом сервере был 3.14),
   `pip install -r requirements.txt` прошёл без единой ошибки.
4. **Прогон тестов на самом сервере: 644 passed** — совместимость с 3.12
   подтверждена не предположением, а запуском.
5. Юнит `content-bot.service` скопирован со старого сервера, `enable`.
6. Старый бот: `stop` + `disable` (проверено: `inactive`, `disabled`).
7. **После остановки** старого — повторный перенос `bot.db` и `site_media`,
   md5 обеих копий совпал (`f503e91e…`), потери данных исключены.
8. Запуск нового: `active`, в логе `Бот запускается (long polling)`,
   `getMe` → `copiwriter_writer_smm_bot`, два успешных вызова `get_balance`
   к vsegpt и ожидаемое `AI proxy balance below alert threshold`.
9. nginx + Let's Encrypt на `159-194-214-72.sslip.io`, автопродление
   (`certbot.timer`) активно. Проверено живыми запросами:
   `/content/services/card_1` → 200 с реальным текстом из перенесённой базы,
   `/media/services/card_1.jpg` → 200.
10. `authorized_keys` со старого сервера перенесён целиком, поэтому ключ
    `github-actions-deploy` на новом сервере уже работает.

Простоя фактически не было: бот всё равно не мог генерировать из-за пустого
баланса.

### Что осталось владельцу

- **Пополнить vsegpt.ru** — https://vsegpt.ru/User/Money. Без этого бот
  принимает сообщения, но на генерации отвечает «сервис временно
  недоступен».
- **Поменять секрет `DEPLOY_HOST` на GitHub** с `147.45.175.189` на
  `159.194.214.72`, иначе автодеплой продолжит молча ездить на старый
  сервер: https://github.com/olgashomrina/BOT-AGENT-PO-SOZDANIYU-KONTENTA-DLYA-SOCSETYEY/settings/secrets/actions
- **Поправить сайт**: в `services.html` репозитория `my-lending-test`
  заменить `data-api-base="https://147-45-175-189.sslip.io"` на
  `https://159-194-214-72.sslip.io`. В репозитории бота примеры уже
  обновлены.
- **Старый сервер Timeweb** оплачен до 14.09.2026 и сейчас простаивает
  (бот на нём остановлен и отключён из автозапуска, данные не тронуты —
  это готовый откат). Удалять не раньше, чем новый отработает несколько
  дней.
- **Подписка vsegpt.ru** активна до 22.08.2026 — если провайдер в итоге
  меняется, автопродление стоит отменить.

### Кто из провайдеров вообще доступен с российского сервера

Проверено с прода (Beget, Москва) в тот же день — это оказалось решающим
для всех дальнейших решений:

```
https://api.runware.ai/v1        -> 400  (отвечает по существу, не блокирует)
https://api.elevenlabs.io/v1     -> 302
https://api.openai.com/v1        -> 401
https://api.replicate.com/v1     -> 401
https://openrouter.ai/           -> 403  (блокирует)
```

**Блокирует только OpenRouter.** `400` и `401` означают, что сервис нас
впустил и ответил по существу («кривой запрос», «нужен ключ»), а не отрезал
по адресу. То есть переезд ради картинок и кружков не нужен — нужен он был
бы только ради OpenRouter.

### Расшифровка переведена на дешёвую модель

Решение от 30.07 («переключаем на `gpt-4o-mini-transcribe`») до прода так и
не доехало: там оставался `whisper-1`. Применено, `.env` скопирован перед
правкой, бот перезапущен и работает. **1.20 → 0.96 ₽/мин, минус 20 % на
голосе бесплатно.**

### Runware для картинок — считаем

Цены (курс 92 ₽/$, как в `USD_RUB_RATE`):

| Провайдер и модель | Цена | В рублях |
|---|---|---|
| Runware, FLUX Schnell | $0.0006 | **0.06 ₽** |
| Runware, FLUX Dev | $0.0038 | **0.35 ₽** |
| OpenRouter, `gpt-5-image-mini` | $0.0085 | 0.78 ₽ (замерено 31.07) |
| vsegpt, `img-flux/flux-2-klein-4b` | — | 3.90 ₽ (сейчас) |
| vsegpt, `img-flux/pro1.1` | — | ~10–15 ₽ |

Полный цикл «голосовое 1 мин → 2 поста → картинка»: **1.56 ₽ вместо 5.35 ₽**,
то есть ~128 циклов на 200 ₽ вместо ~37. Экономия целиком приходит с картинок
— там 70–90 % чека.

Подвох: у Runware **свой формат API** (`taskType: "imageInference"`, массив
задач, ответ `imageURL`/`base64Data`), не openai-совместимый. Нужен отдельный
переходник в `ai_gateway` — примерно полдня работы. Новым аккаунтам дают $2
кредитов, то есть проверить можно, ничего не заплатив.

Оговорка по качеству: FLUX хуже рисует надписи внутри картинки, чем модели
OpenAI. Под картинки с русским текстом стоит оставить дорогую модель.

### Цены на текст у vsegpt (живой каталог, за 1000 токенов, промпт/ответ)

```
openai/gpt-4o-mini  0.02 / 0.08   ~0.25 ₽ за цикл (сейчас)
openai/gpt-5-mini   0.037 / 0.30  ~0.8 ₽ за цикл
openai/gpt-5        0.15 / 1.30   ~3.5 ₽ за цикл
openai/gpt-4o       0.6 / 1.8     ~5-6 ₽ за цикл
```

Текст — меньше 5 % чека, поэтому на нём разумнее не экономить, а доплатить за
качество: `gpt-5-mini` стоит полрубля за цикл. Предложено попробовать и
сравнить на реальных постах, решения пока нет.

### Кружочки: другой ценовой класс

Считать надо до того, как строить этап 2, а не после:

- **ElevenLabs** — профессиональное клонирование голоса требует тарифа Creator,
  **$22/мес ≈ 2000 ₽**, в него входит 100 000 символов озвучки (~160 кружков),
  сверх — $0.30 за 1000 символов.
- **Липсинк** — по разведке этапа 1 Kling LipSync через fal.ai, ≈$0.45 ≈ **41 ₽
  за кружок**. У Runware есть свой Lipsync 2 Pro, цену не публикуют — надо
  мерить на бесплатных $2.

Итого кружок ≈ **45–60 ₽**: как 130 картинок или 30 постов. При ориентире
«200 ₽/мес на тесты» три кружка съедают весь бюджет. Перед этапом 2 нужно
решить про лимиты и платность функции.

**Найдено попутно:** в проде `ELEVENLABS_API_KEY` не задан, то есть этап 1
(сбор доноров и клонирование голоса) на живом сервере не работает вовсе —
попробовать качество клонированного голоса нельзя, пока ключ не появится.

### Решение: два сервиса одновременно

Владелец спросила, можно ли пользоваться двумя сервисами сразу. Можно, и
именно так и делаем — с разнесением провайдера по операциям (сейчас в коде
провайдер один на всё, это надо доработать; для Runware механизм всё равно
понадобится):

1. **Картинки → Runware** напрямую, без ухищрений.
2. **Тексты и голос → OpenRouter через переходник на Cloudflare Workers**
   (`deploy/cloudflare-worker-openrouter.js`). Бесплатно, карта не нужна.
   Главное тут не 8 копеек экономии на тексте, а то, что это **распечатывает
   уже оплаченные 1721 ₽ на OpenRouter** — тогда vsegpt можно вообще не
   пополнять. На голосе разница заметная: 0.96 → 0.28 ₽/мин.
3. **vsegpt остаётся запасным** — переключение обратно это одна строка.

### Где остановились (конец сессии 03.08)

**Следующий шаг — за владельцем:** зарегистрироваться на cloudflare.com,
создать Worker, вставить в него код из `deploy/cloudflare-worker-openrouter.js`,
нажать Deploy и прислать адрес вида `https://ИМЯ.ПОДДОМЕН.workers.dev`.
Проверка живости: открыть этот адрес в браузере, должно появиться `proxy ok`.

Дальше ассистент проверяет с сервера `curl ADDR/v1/models` (ожидание — 200,
а не 403), после чего ключ OpenRouter кладётся в серверный `.env` через
файловый менеджер Beget (в чат ключ не присылать), бот переключается и
проверяется генерация.

## 2026-08-04

### Переходник на Cloudflare не работает — и это доказано, а не предположено

Воркер создан владельцем (`https://openrouter-proxy.olgashomrina.workers.dev`),
код — `deploy/cloudflare-worker-openrouter.js`. Сам по себе он исправен:
`/` отдаёт `proxy ok`, `/v1/models` проксируется как надо.

Но запрос к OpenRouter через него с прода получил тот же `403 {"success":
false, "error": "Access denied by security policy."}`, что и напрямую.
Первая гипотеза — воркер пересылал все входящие заголовки, включая
`cf-connecting-ip` и `cf-ipcountry`. Переписано на белый список
(`authorization`, `content-type`, `accept`, `http-referer`, `x-title`) —
**не помогло, снова 403**.

Решающая проверка: **тот же адрес воркера, запрошенный из-за рубежа, отдал
200 и полный список моделей.** Значит дело не в коде и не в заголовках:
Cloudflare сам сообщает получателю страну исходного посетителя, когда
получатель тоже стоит за Cloudflare. OpenRouter стоит. Из кода воркера на
это повлиять нельзя.

**Вывод: Cloudflare Workers не обходят эту блокировку. Cloudflare AI Gateway
почти наверняка тоже (тот же Cloudflare, та же передача адреса) — не тратить
на него время.** Файл воркера оставлен в репозитории с предупреждением в
шапке, чтобы к затее не возвращались.

### Решение владельца: путь А — сервер в Казахстане

Из двух оставшихся вариантов («сервер вне РФ» или «остаться на vsegpt и
пополнять») выбран первый. Расклад, на котором принималось решение:

- сервер Beget в Казахстане — 11 ₽/день ≈ 330 ₽/мес;
- распечатываются 1721 ₽, уже оплаченные на OpenRouter;
- расшифровка дешевеет втрое: 0.96 → 0.28 ₽/мин;
- vsegpt не пополняется вовсе, остаётся запасным;
- московский VPS удаляется, остаток по посуточной тарификации остаётся на
  счету аккаунта и уходит на новый сервер.

Картинки на Runware от этой развилки не зависят: Runware отвечает и с
российского адреса.

### Казахстан у Beget распродан — идём в Timeweb

Мощности в казахстанской локации Beget закончились, заказать нельзя.
Пересобранный план: у Timeweb Cloud пять зарубежных локаций — Амстердам,
Франкфурт, Алматы, Нью-Йорк, — а аккаунт там уже есть (на нём старый сервер,
оплаченный до 14.09), карта привязана, тарификация почасовая.

**Следующий шаг — за владельцем:** создать в timeweb.cloud облачный сервер,
локация **Амстердам** (годятся также Франкфурт и Алматы), Ubuntu 24.04,
минимальная конфигурация **не меньше 2 ГБ памяти**, SSH-ключ выбрать из
сохранённых в аккаунте либо вставить `content-bot`. Прислать IP.

**Порядок жёсткий: сначала проверка, потом перенос.** Timeweb — российская
компания, её европейские адреса теоретически тоже могут быть в блок-листе.
Проверка та же:

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://openrouter.ai/api/v1/models
```

200 — переносим бота (процедура отработана 03.08, около часа, от владельца
ничего не требуется) и переключаем текст и голос на OpenRouter. 403 — сервер
сразу удалить (почасовая оплата, потеря копеечная) и брать другого
провайдера: VDSina (Амстердам, от 150 ₽/мес), FirstVDS, SpaceWeb, HOSTKEY —
все с оплатой российскими картами, но там нужна новая регистрация.

Московский сервер Beget не удалять, пока новый не отработает.

### Runware проверен живым ключом: тексты есть, картинкам нужен порог $5

Владелец зарегистрировалась, ключ лежит в серверном `.env` как
`RUNWARE_API_KEY`. Проверено с прода (Москва), поэтому попутно ещё раз
подтверждено: **Runware с российского адреса работает**.

Что выяснено запросами, а не из документации:

1. **`GET /v1/models` → 200, 34 текстовые модели.** Есть и открытые
   (`qwen3.5-4b`, `deepseek-v4-flash`, `google-gemma-4-31b`,
   `openai-gpt-oss-120b`, `zai-glm-5-2`), и закрытые (GPT-5.x, Claude,
   Gemini, Grok).
2. **Текстовый endpoint действительно говорит на протоколе OpenAI.**
   `POST /v1/chat/completions` с `qwen3.5-4b` вернул нормальный ответ —
   значит наш код заработает с ним без переделки, меняются адрес, ключ и
   слаг модели. Ответ приходит в поле `reasoning`, а `content` у этой модели
   пустой — при подключении надо будет это учесть.
3. **Ограничение, которого нет в рекламе:** запрос к `deepseek-v4-flash`
   вернул `400 External inference tasks require a paid invoice or at least
   $5 credit`. То есть часть моделей (по крайней мере «внешние») требует
   баланса от $5.
4. **Картинки на бесплатных кредитах не проверить:** `imageInference`
   (`runware:100@1`, FLUX schnell) вернул `Insufficient available balance`.
5. **Приветственные кредиты оказались $0.05, а не $2**, как обещано на
   сайте. Этого хватило ровно на одну текстовую генерацию открытой моделью.
6. **Расшифровки голосовых в openai-совместимом виде у них нет.**
   `POST /v1/audio/transcriptions` с файлом отвечает `invalidJSONSyntax` —
   то есть такого маршрута не существует, запрос падает в общий JSON-обработчик.
   В документации `transcription` описан как taskType **для видео**
   («extracts speech audio from video files»). Для голосовых Telegram это
   надо проверять отдельно, уже с балансом.

**Вывод:** чтобы двигаться дальше, нужно пополнить Runware на минимальные
**$5 ≈ 460 ₽**. Это не расход впустую: $5 — это примерно 1300 картинок
FLUX Dev (0.35 ₽) или 7600 FLUX Schnell, плюс тексты, которые на их
открытых моделях стоят копейки. После пополнения проверяются картинки,
закрытые модели и расшифровка.

Расшифровка, скорее всего, останется на vsegpt (0.96 ₽/мин) — значит
небольшое пополнение vsegpt всё-таки понадобится, но уже не на картинки.

### Секреты в `.env` заменены

В чат попал скриншот открытого `.env` с настоящими `BOT_TOKEN` и
`AI_PROXY_API_KEY`. Оба немедленно перевыпущены владельцем (@BotFather →
Revoke current token; новый ключ в кабинете vsegpt), бот перезапущен и
проверен: `getMe` → 200, `copiwriter_writer_smm_bot`, сервис `active`.
Правило то же, что и после прошлого случая: **`.env` не показывать
целиком — ни скриншотом, ни копированием.**

### Runware проверен на пополненном балансе — что работает, а что нет

**Минимальное пополнение у Runware — $20**, вариант с $5 в кабинете не
предлагается (сообщение об ошибке про «$5 credit» вводит в заблуждение).
Владелец пополнила на $20. Дальше всё проверено живыми запросами с прода.

**Картинки — работают, дешевле, чем считали по прайсу:**

| Модель | AIR-идентификатор | Цена по факту |
|---|---|---|
| FLUX.1 Schnell | `runware:100@1` | $0.0006 = **0.06 ₽** |
| FLUX.1 Dev | `runware:101@1` | $0.0013 = **0.12 ₽** |

Обе картинки (1024×1024, портрет женщины 40+ в осеннем парке) —
фотореалистичные и годные в пост без правок; Schnell при этом выглядит
даже резче Dev. Против 3.90 ₽ у vsegpt это **дешевле в 32–65 раз**.

**Тексты — работают по протоколу OpenAI, код менять не придётся.**
`POST /v1/chat/completions`, модель `deepseek-v4-flash` написала живой
русский пост с эмодзи и вопросом в конце. Расход: 58 токенов запроса,
165 ответа — около **0.002 ₽ за пост**.

Подвох для переходника: модели OpenAI (`openai-gpt-5-4-mini` и, вероятно,
все `gpt-5*`) отвергают параметр `max_tokens` и требуют
`max_completion_tokens`. У `deepseek-v4-flash` этой проблемы нет.
У `qwen3.5-4b` ответ приходит в поле `reasoning`, а `content` пустой.

**Расшифровки голосовых нет.** taskType `audioTranscription` в списке
поддерживаемых есть (полный список получен из ошибки `invalidTaskType`),
но запрос к нему возвращает пустой `data: []`, а в каталоге аудиомоделей
(21 штука) **ни одной модели распознавания речи** — только TTS, музыка и
звуковые эффекты. Вывод: **расшифровка остаётся на vsegpt** (0.96 ₽/мин),
небольшое пополнение vsegpt всё-таки нужно.

### Неожиданная находка для кружочков

В каталоге Runware есть ровно то, на что в этапе 2 закладывались
ElevenLabs ($22/мес) и fal.ai ($0.45 за кружок):

- **Клонирование голоса и TTS:** `alibaba:qwen@3-tts-1.7b-customvoice`
  (CustomVoice — это клонирование), `alibaba:qwen@3-tts-1.7b-voicedesign`,
  `fishaudio:s2.1@pro`, `minimax:speech@2.8`, `xai:tts@0`,
  `google:gemini@3.1-flash-tts`, `inworld:tts@2`.
- **Липсинк и говорящие аватары:** `heygen:avatar@5`, `sync:3@0`
  (это sync.so), `prunaai:p-video@avatar`, `prunaai:p-video@animate`,
  `klingai:kling-video@3.0-turbo`.

То есть кружочки, возможно, целиком собираются на одном Runware — без
абонентской платы ElevenLabs. Цены не замерялись, это задача этапа 2, но
проверять теперь надо в первую очередь Runware, а не fal.ai.

### Итоговая расстановка

| Что | Кто | Цена |
|---|---|---|
| Картинки | **Runware**, `runware:100@1` | 0.06 ₽ |
| Тексты | **Runware**, `deepseek-v4-flash` | ~0.002 ₽ за пост |
| Расшифровка | vsegpt, `gpt-4o-mini-transcribe` | 0.96 ₽/мин |

Цикл «голосовое 1 мин → 2 поста → картинка»: **~1.02 ₽ вместо 5.11 ₽**.
Причём почти весь остаток — это расшифровка; всё, что делает Runware,
стоит меньше десятой доли рубля.

### Runware подключён и работает на проде

Провайдер теперь выбирается **на операцию**, а не на весь шлюз. Иначе
пришлось бы выбирать между дешёвыми картинками и наличием расшифровки:

| Операция | Провайдер | Модель | Цена |
|---|---|---|---|
| Картинки | runware | `runware:100@1` | 0.06 ₽ |
| Кнопка «Сделать реалистичнее» | runware | `runware:101@1` | 0.12 ₽ |
| Тексты постов | runware | `deepseek-v4-flash` | ~0.002 ₽ |
| Расшифровка голоса | vsegpt | `stt-openai/gpt-4o-mini-transcribe` | 0.96 ₽/мин |

Настройки: `IMAGE_PROVIDER`, `TEXT_PROVIDER`, `RUNWARE_*` — описаны в
`.env.example`. Пустое значение означает «как основной провайдер», поэтому
конфигурация без них ведёт себя ровно как раньше.

**Что пришлось написать, а что нет.** Текстовый эндпоинт Runware
openai-совместим, поэтому для текста меняются только адрес, ключ и модель —
ни строчки в разборе ответа. Картинкам понадобился отдельный переходник:
массив задач вместо openai-совместимого тела, картинка в `imageBase64Data`,
а про неудачу Runware сообщает массивом `errors` **при HTTP 200** — то есть
тело надо разбирать даже при успешном коде.

**Две ловушки, найденные по дороге:**

1. Кнопка «Сделать реалистичнее» (`bot/handlers/refine.py`) просит модель
   слагом vsegpt. Для Runware это бессмысленная строка, и кнопка сломалась бы
   ровно у того, кто захотел качество получше. Чужие слаги теперь переводятся:
   премиальный — в премиальный Runware, любой другой — в модель по умолчанию.
2. `.env.example` предлагает оставлять необязательные строки пустыми, а
   `os.environ.get` возвращает для них пустую строку, а не значение по
   умолчанию. Пустой `RUNWARE_BASE_URL` — это сломанный адрес запроса. Теперь
   пустое значение означает «не задано».

Тесты: **655 зелёных** (было 644). Сквозная проверка на самом проде через
код бота: текст сгенерирован, картинка получена (JPEG, 244 КБ), сервис
`active`.

**Осталось по этой теме:**

- **Пополнить vsegpt** — расшифровка голосовых идёт через него, а там
  0.0177 ₽. Тексты и картинки уже не зависят от vsegpt.
- Предупреждение о низком балансе следит только за vsegpt; баланс Runware
  бот не отслеживает. Пока баланс $20 это не горит.
- Прод обновлён копированием файлов, поэтому его рабочая копия расходится с
  git. Перед первым автодеплоем (после смены `DEPLOY_HOST`) на сервере
  нужно будет сбросить локальные изменения, иначе `git pull` упрётся в них.
